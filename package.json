{
  "name": "jsonlify",
  "version": "1.0.0",
  "description": "Convert course content from JSON to JSONL",
  "scripts": {
    "build": "npx tsc && copyfiles -u 1 src/**/*.json dist",
    "start": "npm run build && node dist/index.js"
  },
  "author": "Anton Venema",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.10",
    "copyfiles": "^2.4.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "chalk": "4.1.2",
    "commander": "^12.1.0"
  }
}
